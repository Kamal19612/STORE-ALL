-- STORE-ALL : tables métier créées / mises à jour par l’API Spring (Hibernate) sur la même instance Postgres.
-- Ce fichier existe pour satisfaire [db.seed].sql_paths dans config.toml ; pas de données seed obligatoires ici
-- (évite une deuxième source de vérité pour le catalogue / les comptes staff).
SELECT 1;
